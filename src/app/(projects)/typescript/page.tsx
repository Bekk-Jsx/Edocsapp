import { EmptyProject } from "@/components/ui/empty-project";

// Stub landing — no content yet. The markup that used to live here is now
// ui/empty-project, shared by every project in this state.
export default function Page() {
    return <EmptyProject slug="typescript" />;
}
