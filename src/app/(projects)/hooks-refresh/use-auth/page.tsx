// Placeholder route: the custom-hook pattern (useContext wrapped in a guarded
// hook) gets built here later. No PageShell, demo or content yet — the registry
// entry links somewhere real in the meantime.
export default function Page() {
    return (
        <div className="max-w-3xl">
            <h1 className="text-3xl font-semibold">useAuth</h1>
            <p className="mt-3 text-[var(--muted)]">Coming soon.</p>
        </div>
    );
}
