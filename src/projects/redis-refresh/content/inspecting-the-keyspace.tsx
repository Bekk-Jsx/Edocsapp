import { DocSection } from "@/components/ui/doc-section";

// Placeholder. The real notes replace the single section below — same shape as
// the hooks content files, so sections just get added around it.
export function InspectingTheKeyspaceDocs() {
    return (
        <DocSection title="coming soon">
            <p>Notes for this topic land here.</p>
        </DocSection>
    );
}
